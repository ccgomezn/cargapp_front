const rowStyle = {
  width: '100%',
  display: 'flex',
  flexFlow: 'row wrap',
};
const colStyle = {
  marginBottom: '10px',
};
const gutter = 10;
const basicStyle = {
  rowStyle,
  colStyle,
  gutter,
};

export default basicStyle;
